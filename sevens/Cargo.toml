[workspace]
resolver = "2"
members = [
  "v3/crates/sevens-federation-peer",
  "v3/plugins/gastown-bridge",
]

# sevens is primarily TypeScript; this workspace manifest exists so the
# repo-scorecard analyzer sees sevens's Rust components (federation peer
# binary, gastown-bridge plugin) without traversing deep subdirs.
